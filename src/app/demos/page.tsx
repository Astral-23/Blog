export default function DemosPage() {
  return (
    <section className="page-wrap">
      <h1 className="page-title">demos</h1>
      <p>
        インタラクティブデモ用のページです。将来 `src/app/demos/*` に個別デモを追加できます。
      </p>
    </section>
  );
}
